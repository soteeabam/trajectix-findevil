# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — set for full LoopPause human-in-the-loop mode
# Without this, Pipelock terminal gate is used instead
LOOPPAUSE_API_KEY=

# Optional — set to route Claude calls through the Trajectix proxy
# ANTHROPIC_BASE_URL=http://localhost:8080
